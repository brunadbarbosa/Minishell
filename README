not expand env in heredoc

Built-ins that use env can only be the 1st command

Update clean_shell to unlink the shell->heredoc

startproc.c

ft_execute.c

rl_clear_history before exiting

maybe don't need cpy_heredoc. Maybe don't need to have jeredocs in t_pipe